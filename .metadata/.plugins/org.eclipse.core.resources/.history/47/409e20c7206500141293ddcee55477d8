
public class RomanConverter {

	private String roman;
	private Integer number;
	
	public RomanConverter(){
		roman = "";
		number = 0;
		}
	public RomanConverter(String theRoman, Integer theNumber){
		roman = theRoman;
		number = theNumber;
	}
	public Integer RomanToDecimal(String theRoman){
		Integer num = 0;
		Integer length = theRoman.length()-1;
		for(int i = length; i >= 0; i--){
			if(i == length){
				switch (theRoman.charAt(length)){
				
				case 'I':
					num += 1;
				case 'V':
					num += 5;
				case 'X':
					num += 10;
				case 'L':
					num += 50;
				case 'C':
					num += 100;
				case 'D':
					num += 500;
				case 'M':
					num += 1000;
				}
			}
		
			else{
				if(theRoman.charAt(i) == theRoman.charAt(i+1)){
					switch (theRoman.charAt(i)){
					
					case 'I':
						num += 1;
					case 'V':
						num += 5;
					case 'X':
						num += 10;
					case 'L':
						num += 50;
					case 'C':
						num += 100;
					case 'D':
						num += 500;
					case 'M':
						num += 1000;
				}
					else if (theRoman.charAt(index))
					
					
			}
		}
	}
}
